#include <bits/stdc++.h>

using namespace std;

void print(vector<int>& a){
    for(int i = 0; i < a.size(); ++i){
        if(i != 0) cout << " ";
        cout << a[i];
    }
    cout << "\n";
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    
}