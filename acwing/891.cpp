#include <bits/stdc++.h>
using namespace std;
#define int long long
const int maxn = 1e5 + 1;
int n;
int a[maxn];

signed main() {
    #ifdef LOCAL
    freopen("in", "r", stdin);
    #endif
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> n;
    for(int i = 0 ;i < n; ++i) cin >> a[i];

}