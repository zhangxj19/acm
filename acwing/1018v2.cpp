#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e2 + 5, M = 2e2 + 5;
int n;
int a[N][N];
int f[N][N][M];

signed main() {
    #ifdef LOCAL
    freopen("in", "r", stdin);
    #endif
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> n; 
    for(int i = 1; i <= n; ++i) {
        for(int j = 1; j <= n; ++j) {
            cin >> a[i][j];
        }
    }

    for(int i = 1; i <= n; ++i) {
        for(int j = 1; j <=n; ++j) {
            
        }
    }


    
}